<ol class="post-list">
  {{#each posts as |post|}}
  <li>
    <article class="post">
      <div class="post-heading">
        <a href="" target="_blank">{{post.title}}</a> by {{post.user.username}}, {{post.created_at}}
      </div>
    </article>
  </li>
  {{/each}}
</ol>